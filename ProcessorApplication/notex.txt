7. Run on Any PC (No IIS, No EXE UI), Kestrel execution
Publish as self-contained
bashdotnet publish -c Release -r win-x64 --self-contained true
# or linux-x64, osx-x64

todos:
- use granular headers in tunneling http requrests in providerless module
- make scripts into a cleanup-able object function that will cleanup after itself

